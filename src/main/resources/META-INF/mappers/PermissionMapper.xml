<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.flat.head.dal.mappers.PermissionMapper">

    <select id="getPermTokenIdByToken" resultType="java.lang.String">
        select permission_id from sys_acl_permission where perm_token=#{token}
    </select>
</mapper>